<h2>Popular Products</h2>
<ul>
    @foreach (var product in Model)
    {
        <li>@product</li>
    }
</ul>
<button onclick="location.href='@Url.Action("ClearCache", "Cache")'">Clear Cache</button>
